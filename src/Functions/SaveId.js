/*
    SaveId takes the IdMetaData
    1. savesit into the database using setDoc to specify key
    2. use email as the key
    props.Id is argument
*/

function SavedId(props) {


}

export default SavedId;